namespace VendorRisk.Domain.Enums;

/// <summary>Overall qualitative risk band derived from the numeric risk score.</summary>
public enum RiskLevel
{
    Low = 0,
    Medium = 1,
    High = 2,
    Critical = 3,
}
